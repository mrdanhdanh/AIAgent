---
failure_id: BUG-0004
task: "Nâng cấp section /team-analyze-failure — sửa bugs: hash không deterministic, taxonomy lệch, contract drift"
attempts:
  - attempt: 1
    error: "error_hash = SHA256(normalized) giao cho LLM tính tay — LLM hallucinate, hash khác nhau mỗi lần. BUG-0001/2/3 đã phải lưu slug ('stale_skill_context') vì không có cách nào ra SHA256 thật → memory search theo hash chết vĩnh viễn."
    error_hash: "llm-deterministic-gap"
    error_type: "LLMDeterministicComputeGap"
  - attempt: 2
    error: "Contract drift 3 nơi: command dùng analysis_status vs agent dùng status; root-cause-agent.yaml mong error_analysis không tồn tại trong output; edge case trả suggestions string thay vì object; taxonomy thiếu type thực tế đã ghi (StaleSkillContext, AutomaticVariableShadowing)."
    error_hash: "contract-drift-v1"
    error_type: "ContractDrift"
  - attempt: 3
    error: "Writer attribution sai: knowledge-failure.md + types.yaml khai failure-agent 'viết failure' nhưng agent edit:deny; search bỏ sót patterns/; failures/README.md mô tả format list trong khi record thật dùng frontmatter."
    error_hash: "memory-schema-drift"
    error_type: "MemorySchemaDrift"
error_detail: "failure-system-determinism"
final_solution: "1) failure-analyzer.ps1 — normalize+SHA256 deterministic, failure-agent bash:allow chỉ chạy script này. 2) Contract v2 single-source-of-truth (command+agent+contract yaml đồng bộ). 3) Taxonomy 2 cấp (enum + detail slug) kèm retryable decision table. 4) Search failures/+lessons/+patterns theo hash+type+tag+token. 5) Writer attribution đúng: learning-agent ghi record."
root_cause: "Đặt phép tính deterministic (hash) lên LLM vốn không deterministic; docs viết tay qua nhiều phase không có single-source-of-truth; không có validation contract vs implementation."
lesson_id: "LSN-GEN-001"
pattern_id: "PAT-003"
lesson: "Mọi phép tính deterministic (hash/normalize/parse) phải nằm trong script — LLM chỉ gọi, không tự tính. Contract command/agent/engine là 1 nguồn sự thật, đổi ở đâu phải đổi cả 3 + chạy /team-syncdocs."
tags: ["workflow", "failure-system", "determinism", "contract"]
reusable: true
created_at: "2026-08-05T00:00:00Z"
resolved_at: "2026-08-05T00:30:00Z"
---
