---
failure_id: BUG-0003
task: "Kiểm tra sức khỏe hệ thống AI Agent Framework qua /team-doctor"
attempts:
  - attempt: 1
    error: "Doctor script (quick scan) in banner 'Doctor v1.0.0 - mode: quick' rồi treo vô hạn ở module environment — mọi mode. Get-ToolVersion -Command 'python' -Args @('--version') gọi `& python` không tham số, mở REPL chờ stdin."
    error_hash: "args_param_shadowing"
    error_type: "AutomaticVariableShadowing"
final_solution: "Đổi tên tham số $Args → $Arguments trong function Get-ToolVersion (environment.ps1:12) và 3 call sites. Tham số $Args trùng tên automatic variable $Args của PowerShell nên không binding được — inside function $Args luôn rỗng."
root_cause: "Khai báo param([string[]]$Args) — $Args là automatic variable read-only của PowerShell (mảng args của function), không thể dùng làm tên tham số. PowerShell không báo lỗi, param bị bỏ qua âm thầm, splatting @Args rỗng → command chạy không có đối số."
lesson_id: "LSN-PS-001"
pattern_id: "PAT-001"
lesson: "KHÔNG dùng tên automatic variable của PowerShell làm tên tham số hoặc biến (Args, PSItem, _ có context khác, Error, Host, HOME, PID, PWD, etc.). PowerShell không báo lỗi — param chỉ âm thầm không binding, gây bug treo/rác rất khó debug."
tags: ["powershell", "scripting", "doctor", "tool-usage"]
reusable: true
created_at: "2026-08-01T00:25:00Z"
resolved_at: "2026-08-01T00:23:21Z"
---
