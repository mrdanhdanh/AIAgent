---
review_id: REV-20260808-104
spec: SPEC-002
item: W018
name: Workflow Evolution
type: rev1
verdict: CONDITIONAL
date: "2026-08-08"
report_of: docs/governance/reviews/REV-SPEC-002-W018-20260808-221946.md
---

# Review REV-20260808-104 — SPEC-002/W018 Workflow Evolution (rev1 lần 1 — tính count)

## Context

- **Target**: `docs/specs/SPEC-002/W018/evolution.md` (16 sections WVE001–WVE016) + 8 YAML artifacts + `workflow-evolution.schema.json`.
- **Type**: `rev1` — **review chính thức lần 1, tính count** (count 0 → 1).
- **Trạng thái trước review**: lifecycle `Draft`, review.status `NotReviewed`, count `0`, `recheck_required: true` (cascade W007/W009/W011/W012/W014/W015/W016/W017 — TRG-002).
- **review_id**: REV-20260808-104. Xác nhận chưa bị session song song chiếm: history = 102 entries (mới nhất REV-20260808-103), review_log mới nhất = REV-20260808-103 (W017), grep `REV-20260808-104` toàn docs (tracker + history + reports + git log) = 0 matches → **104 khả dụng**.

## SLA (P3)

- TRG-006 (Draft > 30 ngày): **không áp** — draft_days = 0 (created 2026-08-08).
- TRG-007 (Completed > 180 ngày): **không áp** — chưa Completed.
- Target `Draft/NotReviewed/count 0` (không inherited, không Completed) → review bình thường, **PASS/CONDITIONAL sẽ tính count**.

## Verify máy (PyYAML 6.0.3 + jsonschema 4.26.0 + grep + git)

- **YAML parse**: 8/8 PASS, 0 ParserError — **W018 SẠCH lớp CRITICAL** (không có `note:` block mapping như W005–W016 đã fix).
- **jsonschema**: `workflow-evolution.yaml` vs `workflow-evolution.schema.json` Draft202012 strict **FAIL 3 errors** (xem MAJOR #02); schema **META-VALID** (required 17 keys, additionalProperties: false).
- **Validator** `spec002-validator.ps1`: **exit 0** (41/41 — W18-001..005 PASS, 16 sections WVE001–WVE016 đủ).
- **git**: W018 working tree **sạch** (commit 758056e, 0 modified) — mọi artifact đã commit, không thay đổi chưa lưu.

## Nội dung đầy đủ

- **16 sections WVE001–WVE016** contiguous khớp frontmatter.
- **Philosophy 4 / Principles 6 / Scope** reads 6 (`Workflow Event/Metrics/Trace/Audit W011` + `Compliance Report W016` + `Registry W014`) + not_reads 5 — khớp 3 nguồn (md = canonical = scope.yaml).
- **Pipeline 6** (Collect → Analyze → Learn → Propose → Approval Gate → Apply) = `workflow-evolution-pipeline.yaml` 6.
- **Proposal model**: fields 7 / status 6 / impact 4 = `workflow-evolution-proposal.yaml`.
- **Approval** 4 mục nội dung (Low auto / Medium Agent / High+Breaking Human / Audit W011) = `workflow-evolution-approval.yaml` (nội dung khớp, nhưng schema type FAIL — MAJOR #02).
- **Safety 4 / Application 3 / Traceability 4** khớp canonical.
- **Events 5 — 3 nguồn khớp**: md WVE011 (PROPOSED/APPROVED/REJECTED/APPLIED/ROLLED_BACK) = canonical `events` 5 = `workflow-evolution-events.yaml` 5.
- **Metrics 6** = md WVE012 = canonical `metrics` 6 = `workflow-evolution-metrics.yaml` 6.
- **Machine-readable WVE015**: list 9 = thực tế 9 (8 YAML + schema.json), **0 missing 0 extra**.
- **Single-definition**: `WORKFLOW_EVOLUTION_*` chỉ xuất hiện trong W018 (canonical + events.yaml) — **0 collision** toàn hệ thống.
- **Success Criteria WVE016** 6 mục đầy đủ.

## Cross-reference 2 chiều — 0 BAD

- **Upstream** (đều tồn tại): W011 `../W011/observability.md` · W012 `../W012/policies.md` · W013 `../W013/governance.md` · W014 `../W014/registry.md` · W016 `../W016/compliance.md` · S018 `../../SPEC-001/S018/evolution.md` (mẫu) · S013 GV011A (Simulation — governance.md) · Constitution `docs/specs/SPEC-000/`.
- **Downstream** (đều tồn tại): W019 (doctor.md x3 + workflow-doctor.yaml x2 + workflow-doctor-checks.yaml — `W018 Evolution | 6 checks`). W020 **không ref trực tiếp** W018 (dashboard.md không nhắc W018) — không phải bad ref.

## Findings

### MAJOR #01 — TRACEABILITY (POLICY-011): claim "S011 reuse trực tiếp" (claim dự kiến — ĐÚNG)

- **Vị trí**: `evolution.md:105` (WVE011 Evolution Events) + `workflow-evolution-events.yaml:1`.
- **Vấn đề**: S011 `events.yaml:6` chỉ định nghĩa **event model** (fields `[id, type, category, severity, source, correlation_id, execution_id, state_from, state_to, timestamp, lineage]` + taxonomy) — **KHÔNG** định nghĩa 5 event `WORKFLOW_EVOLUTION_*` cụ thể nào (grep `WORKFLOW_EVOLUTION` toàn S011 = 0 matches). Claim "reuse trực tiếp" sai nguồn.
- **Cùng lớp**: W013#01 / W014#02 / W011#03 / W012#08 / W015#03 / W016#01 / W017#01 — đều đã fix theo mẫu.
- **Đề nghị**: `evolution.md:105` → `> W018 định nghĩa 5 event types WORKFLOW_EVOLUTION_* — S011 cung cấp event model (fields, correlation_id).`; `workflow-evolution-events.yaml:1` → `# SPEC-002 W018 - Workflow Evolution Events (5 event types WORKFLOW_EVOLUTION_* — S011 cung cap event model fields, correlation_id)`.

### MAJOR #02 — SCHEMA: `approval[0-2]` parse thành mapping thay vì string

- **Vị trí**: `workflow-evolution.yaml:47-49` (canonical `approval`).
- **Vấn đề**: 3 item `- Low impact: auto-approve (Strict rules).` / `- Medium impact: Agent approval.` / `- High impact / Breaking: Human approval bat buoc.` chứa `: ` → PyYAML parse thành mapping `{Low impact: '...'}` thay vì string; schema `approval.items.type = string` → jsonschema strict **FAIL 3 errors** (`approval[0]`, `approval[1]`, `approval[2]` not of type 'string').
- **Cùng lớp**: W008#02 / W009#04 / W010#02 / W012#02 — đã fix bằng quoted string.
- **Đề nghị**: quote 3 item thành `- "Low impact: auto-approve (Strict rules)."` (tương tự `workflow-evolution-approval.yaml:5-8` cũng nên quote để nhất quán).

### MINOR #03 — version `1.0` ở 7 artifact phụ vs `1.0.0` canonical (kế thừa, không chặn)

- 7 artifact phụ (events, scope, pipeline, proposal, approval, metrics, validation) ghi `version: "1.0"` trong khi canonical `workflow-evolution.yaml` + md ghi `1.0.0`. Không chặn (tiền lệ W017 #02 / W016 #03 / W013 #03).

### INFO #04 / #05 / #06 — kế thừa, không chặn

- INFO #04: frontmatter `evolution.md` thiếu key `version`.
- INFO #05: schema `status` enum `[Draft, Review, Approved, Stable, Deprecated]` thiếu `Frozen` — chờ auto-freeze.
- INFO #06: header md `evolution.md:12` "Trạng thái: Draft" sẽ lệch lifecycle `Review` sau review này — tự hết khi Approved/Frozen.

## Scoring

- **coverage**: 97 (nội dung đầy đủ, machine-readable 9/9, sections 16/16, events 3 nguồn khớp; trừ -3 MINOR #03 version)
- **quality**: 97 (cấu trúc rõ ràng, pipeline/proposal/scope nhất quán md↔yaml; trừ -3 MINOR #03)
- **consistency**: 80 (cross-ref 2 chiều 0 BAD, single-definition 0 collision; trừ -10 MAJOR #01 traceability claim sai nguồn, trừ -10 MAJOR #02 schema)
- **governance**: 80 (naming chuẩn, schema META-VALID nhưng data FAIL contract; trừ -10 MAJOR #01 POLICY-011, trừ -10 MAJOR #02 schema)
- **overall**: 88 = round((97+97+80+80)/4) (floor 88.5 — theo quy ước W017 93.5 → 93)

- **coverage chi tiết**: direct_reference 97, cross_reference 80, governance 80, schemas 90.

## Verdict — CONDITIONAL (Decision Matrix: 0 CRITICAL, 2 MAJOR → CONDITIONAL)

- **Tăng count (type tính count + verdict PASS/CONDITIONAL)**: `NotReviewed → Rev1`, count `0 → 1`, lifecycle `Draft → Review` (auto khi count ≥ 1).
- **KHÔNG auto-freeze** (type rev1 — freeze_rule chỉ áp cho revfull PASS + Completed).
- `recheck_required` W018 giữ **true** (còn 2 MAJOR chưa fix — "con gi treo", chờ fix + review lần 2 hoặc regression/Doctor TRG-002).
- Thêm entry `history[]` W018 (REV-20260808-104, CONDITIONAL) + `review_log`.

## Cascade review

- Mục tham chiếu W018: **W019** (doctor.md x3 + workflow-doctor.yaml x2 + workflow-doctor-checks.yaml — ref trực tiếp `W018 Evolution | 6 checks`) — **đã có `recheck_required: true` từ cascade trước**, giữ nguyên, **không đổi review.status** (TRG-002 — Doctor lập task regression). W020 không ref W018 — không cascade.
- Không cascade mới: 2 MAJOR chỉ sửa claim metadata + quote string — **không đổi event types / key / contract** → downstream không bị ảnh hưởng.

## Ghi chú

- 2 MAJOR: #01 TRACEABILITY (claim "S011 reuse trực tiếp" — đúng như dự kiến, cùng lớp 7 lỗi đã fix W013/W014/W011/W012/W015/W016/W017) + #02 SCHEMA (approval items parse thành mapping — cùng lớp W008/W009/W010/W012).
- Báo cáo chỉ ghi nhận; **không tự sửa nội dung mục** — gợi ý fix, chờ user chấp thuận.

## Next step

- **Sau khi user fix 2 MAJOR** → review lại (rev1 xác nhận fix, không tính count — tiền lệ W017 REV-20260808-103).
- Khi cần hoàn tất W018 (đủ 2-pass): review lần 2 (rev1 thứ 2 tính count) → Rev1 → Completed.
- Mục tiếp theo trong chuỗi SPEC-002: **W019 (Workflow Doctor)** — rev1 (đang Draft/NotReviewed/count 0).
- Khuyến nghị chạy `/doctor` khi muốn kiểm tra health tổng thể + regression TRG-002.
