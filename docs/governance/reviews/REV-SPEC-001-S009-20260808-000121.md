---
name: review-report
description: >
  REV-20260808-001 — review rev1 cho SPEC-001/S009. Không tự sửa — do /review tạo.
agent: general
---

# Review Report — SPEC-001/S009

- **review_id**: REV-20260808-001
- **type**: rev1
- **verdict**: CONDITIONAL
- **lifecycle**: Review → Review (giữ nguyên — chờ Approved qua POLICY-001) · **review.status**: Rev1 → Completed · **count**: 1 → 2
- **score**: coverage 84 / quality 87 / consistency 87 / governance 90 / overall 87
- **coverage**: direct_reference 95 / cross_reference 85 / governance 90 / schemas 60
- **severity_counts**: { CRITICAL: 0, MAJOR: 4, MINOR: 4, INFO: 3 }
- **target_file**: docs/specs/SPEC-001/S009/state-machine.md (+ 15 file machine-readable)

## Phạm vi review

- Type `rev1` — matrix bắt buộc: **coverage + governance** (`review-matrix.yaml`).
- Đối chiếu toàn bộ 16 file S009: `state-machine.md`, `state-machine.yaml`, `states.yaml`,
  `transitions.yaml`, `transition-{types,triggers,guards,matrix}.yaml`, `state-{events,history,metrics}.yaml`,
  `retry-model.yaml`, `replay-model.yaml`, `state-machine-{registry,validation}.yaml`, `state.schema.json`.
- Cross-reference upstream: ENT-014 (S008), FR-018 (S002), RULE-012 (SPEC-000), P005/P006/P013 (SPEC-000), S008, runtime-models.
- Downstream (grep toàn hệ thống): S008, S010, S011, S012, S013, S015.

## Findings

| # | Severity | Category | Location | Mô tả | Gợi ý |
|---|----------|----------|----------|-------|-------|
| #01 | MAJOR | SCHEMA | `transition-matrix.yaml:6-23` | Matrix chỉ có **18** `allowed: true` nhưng `transitions.yaml` khai báo **19** transitions — thiếu **TR-017** (`ST-013 → ST-009`, guard "Hết retry"). Đồng thời thiếu 2 invalid transitions đã khai báo trong `state-machine.yaml invalid_transitions`: `ST-010→ST-013` (Cancelled→Retrying), `ST-014→ST-008` (Aborted→Completed). Doctor đọc matrix này sẽ bỏ sót transition hợp lệ và không biết 2 đường cấm. | Thêm `{ from: ST-013; to: ST-009; allowed: true }` + `{ from: ST-010; to: ST-013; allowed: false }` + `{ from: ST-014; to: ST-008; allowed: false }`. |
| #02 | MAJOR | CONSISTENCY | `state-machine-registry.yaml:9-13` | Registry khai báo `transitions: 18` (đúng phải **19**) và `internal: 4` (đúng phải **3** — type Internal chỉ có ST-002, ST-003, ST-013). Registry là file "Doctor/Dashboard doc mot file" → số liệu sai lan sang doctor/dashboard. | Sửa `transitions: 19`, `internal: 3`. Đối chiếu lại `states.yaml` type counts: Initial 1 / Internal 3 / Active 4 / Terminal 6 = 14. |
| #03 | MAJOR | SCHEMA | `state.schema.json:20-33,37` vs `state-machine.yaml:58-78` | Schema validation vỡ 2 chiều: (a) `transitions.items` yêu cầu **object** `{from,to,event,guard}` nhưng `state-machine.yaml` dùng chuỗi `"from: X; to: Y; event: Z; guard: W"`; (b) `additionalProperties: false` reject các top-level keys thực tế có: `categories`, `active_states`, `control_states`, `concurrency_rules`, `composite_states`, `terminal_rules`, `triggers`, `invalid_transitions`. → `state-machine.yaml` không validate được bằng schema hiện hành. | Cập nhật schema: đổi `transitions.items` thành đa dạng (object hoặc string pattern `^from: .*; to: .*`), và bổ sung các keys thực tế vào `properties` (hoặc bỏ `additionalProperties: false` ở top-level). |
| #04 | MAJOR | COMPLIANCE | `review-history.yaml:6` vs `review-tracker.yaml:105-109` | `review-history.yaml` có `entries: []` (rỗng) nhưng tracker S009.history ghi **REV-20260807-001** (review lần 1). Vi phạm quy tắc nhất quán tracker + history (POLICY-010, `review-rules.yaml` verification) — mất traceability của lần review trước. | Backfill entry REV-20260807-001 (verdict CONDITIONAL, type rev1, ngày 2026-08-07) vào `review-history.yaml` — do Doctor hoặc /review thực hiện. |
| #05 | MINOR | CONSISTENCY | `state-events.yaml:17-18` | Danh sách events 18 entries nhưng 19 transitions sinh event: `EXECUTION_FAILED` chỉ xuất hiện 1 lần (cần cho TR-012 **và** TR-017), trong khi `EXECUTION_REPLAYED` xuất hiện 2 lần (TR-018/TR-019 — hợp lệ). Projection không khớp `transitions.yaml generated_events`. | Thêm `EXECUTION_FAILED` lần 2 (đại diện TR-017) hoặc đổi sang cấu trúc event→transition để hết ambiguity. |
| #06 | MINOR | CONSISTENCY | `states.yaml:41,86` | `events` của state không phản ánh đủ incoming transitions: ST-004 Running chỉ `[EXECUTION_STARTED]` nhưng nhận thêm `EXECUTION_APPROVED` (TR-006), `EXECUTION_RESUMED` (TR-008), `EXECUTION_RETRIED` (TR-016); ST-009 Failed chỉ `[EXECUTION_FAILED]` nhưng nhận `EXECUTION_VALIDATION_FAILED` (TR-003). | Bổ sung đầy đủ events theo transitions.yaml hoặc ghi rõ semantics của trường `events` (enter-event vs all-related). |
| #07 | MINOR | TRACEABILITY | `state-machine.md:257-273,301-316` | `state-machine-registry.yaml` + `state-machine-validation.yaml` tồn tại nhưng không được liệt kê trong section "Machine-readable" và "Tham chiếu" (chỉ liệt kê 13/15 file). Cross-reference 1 chiều — md → file. | Bổ sung 2 file vào danh sách "Machine-readable" + "Tham chiếu" của `state-machine.md`. |
| #08 | MINOR | CONSISTENCY | `state-machine.yaml:1` vs `state.schema.json:4` | Version metadata lộn xộn: comment `state-machine.yaml` ghi "v3 (Canonical)", schema title ghi "S009 v2", còn `version:` là `"1.0.0"` ở cả hai. | Thống nhất: ghi version thật vào comment hoặc bỏ hẳn "vN" trong comment/schema title. |
| #09 | INFO | STRUCTURE | `state-machine-validation.yaml:1` | Comment "ST007" không phải section id chuẩn (SM007 = Transition Matrix; nội dung validation thuộc SM020/SM011). | Sửa comment thành "SM020" hoặc mô tả rõ hơn. |
| #10 | INFO | DEPENDENCY | `S012/policies.md:167` | Downstream tham chiếu sai: "Approval gate (S009 TR-005/006) → action **Waiting (ST-006)**" — theo S009, Waiting = **ST-005**, ST-006 = Suspended. Lỗi thuộc S012 (không phải S009) → đưa vào cascade recheck. | Sửa S012 thành `Waiting (ST-005)` khi regression S012. |
| #11 | INFO | STALE | `state-machine.md:12` + tracker | Trạng thái doc `status: Draft` (frontmatter) vs lifecycle tracker `Review` — hợp lệ (Review = đang review, sẽ về Approved khi POLICY-001). Ghi nhận, không cần sửa. | — |

## Điểm số chi tiết

Deduction theo `review-severity.yaml` (MAJOR −10, MINOR −3, INFO 0), mỗi finding gán vào dimension chính:

- **coverage** = 100 − #01(10) − #05(3) − #06(3) = **84**
- **quality** = 100 − #03(10) − #07(3) − #09(0) = **87**
- **consistency** = 100 − #02(10) − #08(3) − #10(0) = **87**
- **governance** = 100 − #04(10) − #11(0) = **90**
- **overall** = (84×25 + 87×25 + 87×25 + 90×25)/100 = **87**

Coverage metrics: direct_reference **95** · cross_reference **85** · governance **90** · schemas **60**

## Matrix checks (rev1: coverage + governance)

| Dimension | Kết quả | Ghi chú |
|-----------|---------|---------|
| coverage | CONDITIONAL | Scope/structure/content đầy đủ (20 sections, 14 states, 6 categories, 19 transitions) nhưng matrix thiếu TR-017 + 2 invalid (#01) |
| governance | CONDITIONAL | naming/version OK; history desync vi phạm POL-010 (#04) |

## Verdict — Decision Matrix

- CRITICAL: **0** · MAJOR: **4** → verdict **CONDITIONAL** (0 CRITICAL + ≥1 MAJOR).
- CONDITIONAL + type `rev1` (tính count) → tăng count 1→2, `NotReviewed→Rev1→Completed` (ladder `review-status.yaml`).
- Lifecycle giữ nguyên `Review` — Approved chỉ khi review Completed + POLICY-001 approval.

## Cascade Review

- cascade_review: **[S008, S010, S011, S012, S013, S015]** — đã gắn `recheck_required: true` trong tracker cho S008, S010, S011, S012, S013 (S015 chưa có entry tracker — cần thêm).
- **KHÔNG tự đổi** review.status của các mục này — Doctor lập task regression (TRG-002).
- Ghi chú cascade: S010 dùng transition map trực tiếp (ST-002→ST-009, ST-004→ST-014...); S011 dùng State Events projection; S012 tham chiếu TR-005/006/013/015 (đã thấy ST-006 sai → #10); S013 dùng ST-009/ST-014; S015 dùng Terminal State rule; S008 liên kết 2 chiều canonical.

## Trigger nhắc

- SLA: draft_days = **3** (created 2026-08-05 → nay 2026-08-08) — không vi phạm TRG-006 (30 ngày).
- stale_in_days = **180** (Completed hôm nay — TRG-007 áp dụng từ nay, không trigger).
- trigger_notice: **[TRG-002]** — dependency/cascade → Doctor lập task regression cho các mục recheck_required.

## System Analysis

- **cascade_impact**: S008 (2 chiều canonical), S010 (execution flow dùng state map), S011 (state events), S012 (policy trigger/action tham chiếu TR/ST), S013 (governance enforcement → ST-009/ST-014), S015 (resource release on Terminal State).
- **upgrade_suggestions**:
  - `transition-matrix.yaml` — thêm TR-017 + 2 invalid — **P1**
  - `state-machine-registry.yaml` — transitions 18→19, internal 4→3 — **P1**
  - `state.schema.json` — cập nhật transitions format + top-level keys — **P1**
  - `review-history.yaml` — backfill REV-20260807-001 — **P2**
  - `state-events.yaml` / `states.yaml` — bổ sung events đầy đủ — **P2**
  - `state-machine.md` — thêm registry + validation vào Tham chiếu — **P3**
- **missing_items**: S015–S020 chưa có entry trong tracker (S015 có file `resources.md` nhưng tracker thiếu); SPEC-002+ chưa khởi tạo.
- **recommended_next**: S010 rev1 (phụ thuộc trực tiếp, đã recheck_required) → S011 → S012 (fix ST-006) → S013 → S014. Sau đó chạy `/doctor` để health check + sync history.

## Summary & Next Step

**Summary**: S009 — Runtime State Machine được review lần 2 (rev1). Nội dung canonical vững chắc: 14 states, 6 categories, 19 transitions nhất quán giữa `state-machine.md`, `states.yaml`, `transitions.yaml` và upstream (ENT-014/FR-018/RULE-012/P005/P006/P013 đều tồn tại). Tuy nhiên 4 lỗi MAJOR về máy-đọc được: matrix thiếu TR-017 + 2 invalid, registry sai số liệu (18/4), schema không validate được artifact, và history global mất entry lần review 1 — dẫn tới verdict CONDITIONAL, review.status đạt Completed (2 lần review).

**Next step**: ① Áp dụng fix P1 (#01–#03: matrix, registry, schema) + backfill history (#04) sau khi được chấp thuận; ② `/doctor` lập task regression TRG-002 cho cascade (S008/S010/S011/S012/S013/S015); ③ S009 đã Completed — sẵn sàng cho approval POLICY-001 khi các MAJOR được xử lý. Không tự sửa nội dung mục — mọi fix cần chấp thuận rồi review lại.

## Tham chiếu

- Tracker: `docs/governance/review-tracker.yaml` · History: `docs/governance/review-history.yaml`
- Rules/Trigger/Matrix: `docs/governance/review-{rules,trigger,matrix}.yaml` · Severity/Score/Status: `review-{severity,score,status}.yaml`
- Target: `docs/specs/SPEC-001/S009/state-machine.md` + `state-machine.yaml` + 14 file machine-readable khác
