---
name: review-report
description: >
  REV-20260808-080 — review rev1 cho SPEC-002/W008 (Workflow Data Model).
  Verdict FAIL (1 CRITICAL — canonical YAML ParserError). Không tự sửa — do /review tạo.
agent: general
---

# Review Report — SPEC-002/W008

- **review_id**: REV-20260808-080
- **type**: rev1 (review lần 1 — tính count; mục thứ 8 trong chuỗi review SPEC-002)
- **verdict**: **FAIL**
- **lifecycle**: Draft → Draft (giữ nguyên) · **review.status**: NotReviewed → NotReviewed · **count**: 0 → 0
- **score**: coverage 95 / quality 72 / consistency 60 / governance 90 / overall 79
- **coverage**: direct_reference 95 / cross_reference 88 / governance 90 / schemas 30
- **recheck_required**: true (kế thừa cascade W002 — giữ nguyên; cascade W002 chưa regression)

## Findings (severity: CRITICAL/MAJOR/MINOR/INFO)

| # | Severity | Category | Location | Mô tả | Gợi ý |
|---|----------|----------|----------|-------|-------|
| 01 | CRITICAL | Schema | `W008/workflow-data-model.yaml:50` | Canonical YAML **ParserError**: block sequence `dependencies:` (dòng 43-49) + key `note:` cùng indent (dòng 50) → `expected <block end>, but found '?'`. Cùng class W005#01 (REV-20260808-075)/W006#01 (REV-20260808-077)/S005#01/S006#01/S003#01. jsonschema KHÔNG chạy được vì YAML hư. | Chuyển `note:` thành `# note:` (YAML comment) như fix W005/W006 — không đổi dữ liệu |
| 02 | MAJOR | Schema | `W008/workflow-data.schema.json` + `workflow-data-model.yaml:8-12,42` | **Schema contract mismatch** (verify bằng mô phỏng sau khi fix #01): (a) `aggregate_rules[1]/[2]` là dict `{Workflow owns: [...]}`/`{Workflow references: [...]}` nhưng schema khai `items: {type: string}` → fail; (b) `dependencies` chỉ có **7 items** nhưng schema yêu cầu `minItems: 8` (note: không phải dependency) → "is too short". Ngay cả khi YAML parse OK, jsonschema vẫn FAIL 3 lỗi. | Sửa schema: khai `aggregate_rules.items` chấp nhận dict (hoặc chuyển owns/references thành string), giảm `dependencies.minItems` xuống 7 (hoặc bổ sung dependency thứ 8) |
| 03 | MAJOR | Consistency | `W008/data-model.md` WF004 + WF003 + WF010 + `workflow-models/workflow-models.yaml` | **Naming inconsistency**: bảng entity WF004 dùng `ENT-W005 StepCondition`/`ENT-W006 StepGate`/`ENT-W007 StepContext` nhưng cùng md WF003 classification + WF010 relations dùng `Branch`/`Gate`/`WorkflowContext`; appendix canonical (WM-004 Branch, WM-005 Gate, WM-006 WorkflowContext) dùng tên Branch/Gate/WorkflowContext. Tên entity không khớp tên canonical → vi phạm P009 ("không định nghĩa lại"). | Thống nhất tên: dùng `Branch`/`Gate`/`WorkflowContext` cho cả entity table + yaml (theo appendix), hoặc đổi tên appendix — cần quyết định 1 hướng |
| 04 | MINOR | Governance | 9 YAML W008 (`version: "1.0"`) vs `data-model.md:12` (`Version: 1.0.0`) | Version '1.0' ở canonical + 8 artifact phụ vs '1.0.0' trong md header. Cùng class W002#01/W003#01/W004#01/W005#02/W006#03/W007#01. | Nâng `version: "1.0"` → `"1.0.0"` ở 9 YAML |
| 05 | INFO | Governance | `data-model.md:1-8` | Frontmatter thiếu field `version` (md:12 có 1.0.0). Không bắt buộc — cùng class W007#05. | Có thể thêm `version: 1.0.0` vào frontmatter cho nhất quán |
| 06 | INFO | Governance | `workflow-data.schema.json:10` | `status` enum `[Draft, Review, Approved, Stable, Deprecated]` thiếu `'Frozen'` — chuẩn bị cho auto-freeze revfull. Cùng class W007#06. | Thêm `Frozen` vào enum |
| 07 | INFO | Governance | `data-model.md:12` | Header `Trạng thái: Draft` — lần này FAIL nên lifecycle giữ Draft (không lệch); sẽ lệch khi review PASS. | Cập nhật sau khi review PASS |
| 08 | INFO | Schema | `spec002-validator.ps1` W8-003 | Validator exit 0 (41/41) vì dùng regex không parse YAML thật → không bắt được ParserError #01. Cùng class W005#05. | (Tùy chọn) nâng validator dùng PyYAML |

## Cascade Review

- **cascade_review: [W009]** — `W009/state-machine.md:175` ref `../W008/data-model.md` → gắn `recheck_required: true` cho W009 (TRG-002, KHÔNG đổi review.status của W009 — hiện Draft/NotReviewed).
- **W008**: giữ `recheck_required: true` (cascade W002 chưa regression — Doctor lập task TRG-002).

## Trigger nhắc

- **SLA**: TRG-006 không áp (draft_days = 0, created 2026-08-08). TRG-007 không áp (chưa Completed).

## Summary & Next Step

W008 (Draft, NotReviewed, count 0, `recheck_required: true` do cascade W002). Verify máy (PyYAML 6.0.3 + jsonschema 4.26.0 + grep + git): **8/9 YAML parse PASS**, canonical `workflow-data-model.yaml` FAIL ParserError (block sequence + key `note:` cùng indent — dòng 50). jsonschema không chạy được do YAML hư; mô phỏng fix `note:` → vẫn FAIL schema contract (aggregate_rules items là dict, dependencies 7 < minItems 8). Nội dung đầy đủ: 15 entities ENT-W001..015 (md=yaml 15/15), 12 invariants (md=yaml 12/12), classification/lifecycle/ownership/references (4)/relations (6)/validation (7) khớp md↔yaml; machine-readable list 10/10 khớp thực tế; cross-ref 2 chiều 0 bad (upstream W002/S008/S011/S014 + appendix tồn tại; downstream W009 ref tồn tại); grep 'reuse' toàn W008 = 0; git working tree W008 sạch (427cd83, 0 modified). Phát hiện 1 CRITICAL (#01 YAML) + 2 MAJOR (#02 schema contract, #03 naming StepCondition/StepGate/StepContext vs Branch/Gate/WorkflowContext) + 1 MINOR (#04 version) + 3 INFO. Verdict **FAIL** (1 CRITICAL ≥ 1 → Decision Matrix) → **KHÔNG đổi trạng thái**: giữ lifecycle Draft, review.status NotReviewed, count 0, `recheck_required: true`. KHÔNG auto-freeze. Cascade mới: W009 ref W008 → gắn `recheck_required: true` (TRG-002).

**Next step**: User fix 3 lỗi — (#01) `note:` → `# note:` dòng 50 `workflow-data-model.yaml`; (#02) sửa `workflow-data.schema.json` (aggregate_rules items + dependencies minItems); (#03) thống nhất tên entity với appendix. Sau đó chạy lại `/review SPEC-002 W008 rev1` để re-review (mô hình W005/W006).

**Report path**: `docs/governance/reviews/REV-SPEC-002-W008-20260808-194749.md`
