---
name: rev-spec-001-s008-regression
description: >
  AIOS Review Report — SPEC-001/S008 Runtime Data Model, type regression
  (TRG-002 — cascade sau loạt fixes upstream ADR-002..008 + lần đầu machine-validate S008).
agent: review
---

# REV-20260808-034 — SPEC-001/S008 Regression Report

- **review_id**: REV-20260808-034
- **target**: SPEC-001/S008 (Runtime Data Model)
- **type**: `regression`
- **mode**: cascade regression (TRG-002 — sau REV-20260808-001 S009 fix + ADR-002..008 S002/S003/S005/S006/S007 fixes) kiêm lần đầu machine-validate S008
- **verdict**: **FAIL** (1 CRITICAL ≥ 1 — Decision Matrix)

---

## 1. Bối cảnh & mục tiêu

S008 là **Frozen inherited** (lifecycle Frozen, review.status Completed, count 2, inherited: true) với `recheck_required: true` trong tracker — chờ regression từ:

1. **REV-20260807-001** (S009 rev1) `cascade_review: [S008, S010, S011, S012, S013, S015]` — S009 (State Machine) thay đổi → S008 (Data Model dùng state/lifecycle của S009) bị đánh dấu recheck.
2. **Loạt fixes upstream**: S002 (ADR-002 schema), S003 (ADR-003/004 YAML+schema), S005 (ADR-007 YAML+schema), S006 (ADR-006 YAML+schema+rename), S007 (ADR-008 schema), S009 (state-events/transition-matrix/registry/schema), S010/S012..S017...

**Mục tiêu**: verify các fixes upstream không làm vỡ S008 + machine-validate S008 lần đầu (S008 chưa từng được validate bằng máy trong vòng đời review v2.0 — giống S002/S003/S005/S006/S007 từng có defect chưa từng bị phát hiện).

**Lưu ý**: Regression trên mục Frozen/Completed **KHÔNG đổi 2 trục** (lifecycle Frozen, review.status Completed, count 2, inherited) và **KHÔNG tăng count** — chỉ gỡ `recheck_required: true` nếu PASS.

## 2. Phương pháp — verify bằng máy

- PyYAML 6.0.3 (`yaml.safe_load`) — parse toàn bộ 10 YAML artifact S008.
- jsonschema 4.26.0 (`Draft202012Validator`) — strict validate canonical `runtime-data-model.yaml` vs `runtime-data.schema.json` (sau khi fix cú pháp YAML trong bộ nhớ để tách schema defect khỏi parse defect).
- Grep/regex — cross-reference ENT-0xx/RM-0xx, upstream cascade, version/frozen_date.

## 3. Findings

### CRITICAL

| # | ID | Severity | Mô tả | Bằng chứng |
|---|----|----------|-------|-----------|
| 01 | S008-parse | **CRITICAL** | `runtime-data-model.yaml` (canonical — "nguồn dữ liệu chuẩn" data-model.md:293) **KHÔNG parse được**: block sequence `dependencies:` (dòng 45-52) theo sau bởi key `note:` cùng indent (dòng 53) → PyYAML `ParserError: while parsing a block collection ... expected <block end>, but found '?'` | dòng 45-53; `git blame` xác nhận lỗi có từ commit 65d8b61 (2026-08-05) và nằm trong bản freeze 08cc1fa (2026-08-05) — file hong từ lúc freeze, chưa từng machine-validated. **Cùng lớp S003#01 (REV-20260808-008) / S005#01 (REV-20260808-018) / S006#01 (REV-20260808-016)** |

### MAJOR

| # | ID | Severity | Mô tả | Bằng chứng |
|---|----|----------|-------|-----------|
| 02 | S008-schema | **MAJOR** | `runtime-data.schema.json` lỗi thời / không khớp canonical YAML — jsonschema strict FAIL **5 errors** (verify trên bản YAML đã sửa cú pháp in-memory): (a) `additionalProperties: false` chặn `frozen`, `frozen_date`, `status` (3 keys top-level khai báo ở runtime-data-model.yaml:5-7) — **cùng lớp S002#01 (ADR-002) / S003#02 (ADR-003) / S005#02 (ADR-007) / S006#02 (ADR-006) / S007#01 (ADR-008)**; (b) `aggregate_rules[1]` + `[2]` là mapping `{Execution owns: [...]}`, `{Execution references: [...]}` trong khi schema khai báo `items: {type: string}` → 2 errors — **cùng lớp S011#01 / S012#02 / S015#01**; (c) `classification` schema yêu cầu `required: [manages, not_manages]` nhưng YAML dùng cấu trúc `Runtime Data/Execution Data/Transient/Persistent Metadata/Reference Data` + `not_manages` (không có `manages`) → 1 error; (d) `consistency` schema khai báo `type: array` nhưng YAML là object `{levels, rules}` → 1 error | `runtime-data.schema.json` (title "SPEC-001 Runtime Data Model (S008)", v1 cũ); `Draft202012Validator` → 5 errors; git blame: schema từ v1 (65d8b61) chưa theo v2 YAML (e16a45b) |

### MINOR

| # | ID | Severity | Mô tả | Bằng chứng |
|---|----|----------|-------|-----------|
| 03 | S008-version | MINOR | Version không nhất quán: `runtime-data-model.yaml:4` version `"1.0"` vs `data-model.md:12` "Version: 1.0.0" — cùng lớp version 1.0 vs 1.0.0 ở S002/S003/S005/S006/S007 | `runtime-data-model.yaml:4`, `data-model.md:12` |
| 04 | S008-frozendate | MINOR | `frozen_date: "2026-08-04"` (runtime-data-model.yaml:7 + data-model.md:12 "Frozen (2026-08-04)") lệch so với commit freeze thực tế 08cc1fa ngày **2026-08-05** — cùng lớp S003#04 / S004#07 / S005#06 / S007#06 | `git log --format=%ad` commit 08cc1fa = 2026-08-05 |

### INFO

| # | ID | Severity | Mô tả | Bằng chứng |
|---|----|----------|-------|-----------|
| 05 | S008-frontmatter | INFO | Frontmatter data-model.md khai báo "18 sections D001-D018" — thực tế đúng 18 (D001..D018) → OK, không lệch | `data-model.md:5`; regex `^## D\d+` = 18 matches |
| 06 | S008-lifecycle-vs-S009 | INFO | S008 Execution lifecycle `[Created, Prepared, Running, Completed, Archived]` — "Archived" **không** nằm trong S009 states `[Created, Validating, Prepared, Running, Waiting, Suspended, Cancelling, Completed, Failed, Cancelled, TimedOut, Replayed, Retrying, Aborted]`; S008 lifecycle là mức Data (rút gọn) ≠ S009 state machine (chi tiết) — không phải lỗi breaking (S008 refs S009 theo tên, 0 ST-/TR- ID), ghi nhận cho S009 hoàn thiện | `runtime-lifecycle.yaml:6`, `S009/states.yaml` |

## 4. Upstream cascade — verify (OK, không breaking)

| Cascade | Kết quả | Bằng chứng |
|---------|---------|-----------|
| **S009 fixes** (REV-20260808-001: state-events/transition-matrix/registry/schema) | **OK** — S008 refs S009 bằng **tên lifecycle** (runtime-lifecycle.yaml, data-model.md D005/D018), không dùng ST-/TR- IDs trực tiếp (grep `ST-\d+|TR-\d+` toàn S008 = 0 matches) → S009 đổi nội dung state/event không vỡ ref S008. Chiều ngược: S009 state-history.yaml:9 ref **ENT-014** (Execution Lineage) — tồn tại trong runtime-entities.yaml:150 → 2 chiều OK | `runtime-entities.yaml:150`, `S009/state-history.yaml:9` |
| **S002/S003/S005/S006/S007 fixes** (ADR-002..008) | **OK** — S008 refs SPEC-000 principles P003/P004/P009/P010/P017 (đều tồn tại); RM-0xx canonical models (RM-001..012 đều tồn tại trong runtime-models.yaml); không ref trực tiếp S002-S007 bằng section ID (grep EF/OB/POL/GV/RG = 0 matches) | `runtime-entities.yaml`, `runtime-models/runtime-models.yaml` (12 models) |
| **RM-0xx cross-ref** | **OK** — 9/9 RM refs (RM-001, 002, 005, 006, 007, 008, 010, 011, 012) đều tồn tại | `runtime-entities.yaml` vs `runtime-models.yaml` |
| **ENT-0xx cross-ref** | **OK** — 15/15 entities (ENT-001..015) khớp giữa runtime-entities.yaml và data-model.md D001 | `runtime-entities.yaml` (15), `data-model.md` D001 |

**Kết luận cascade**: Không có fix upstream nào làm vỡ S008. Toàn bộ defect tìm thấy là **nội tại S008** (YAML canonical hong + schema lỗi thời) — S008 chưa từng machine-validated, đúng dự đoán (lớp defect giống S002/S003/S005/S006/S007).

## 5. Verdict

**Decision Matrix**: CRITICAL = 1 → **FAIL**

- **Không gỡ** `recheck_required: true` (verdict FAIL — chỉ gỡ khi PASS).
- **KHÔNG đổi 2 trục**: lifecycle **Frozen**, review.status **Completed**, count **2**, inherited **true** (regression trên Frozen/Completed không đổi trạng thái, không tăng count).
- **Không cascade mới** (cascade_review []): findings nội tại S008, không đổi key ENT-0xx/RM-0xx → downstream (S009 ENT-014, S010 artifact-flow, S011 events/metrics, S014 registry, S015 resources, SPEC-002 W008) ref theo tên/ID vẫn đúng.

## 6. Score

Weights 25/25/25/25 (review-score.yaml). Deduction: CRITICAL -30, MAJOR -10, MINOR -3, INFO 0 (review-severity.yaml).

| Dimension | Score | Ghi chú |
|-----------|-------|---------|
| coverage | 90 | Scope/structure đầy đủ (18 sections, 15 entities, 10 YAML + schema), content canonical bị chặn bởi parse error |
| quality | 60 | CRITICAL YAML parse (-30) + MAJOR schema (-10) |
| consistency | 90 | Cross-ref 2 chiều tốt; MINOR version (-3) |
| governance | 90 | MINOR version (-3) + MINOR frozen_date (-3) |
| **overall** | **83** | round((90×25 + 60×25 + 90×25 + 90×25)/100) = round(82.5) |

## 7. SLA

- S008 Frozen → **TRG-006 (Draft SLA 30 ngày): không áp dụng**.
- **TRG-007** (Completed review cycle 180 ngày): created 2026-08-04 → còn ~176 ngày, chưa trigger.

## 8. Next step

**Cần ADR mới** (precedent ADR-002/003/006/007/008) — mo Frozen S008:

1. **#01 CRITICAL**: fix `runtime-data-model.yaml:53` — chuyển `note: Không dependency ngược.` thành comment (`# note: ...`) hoặc đưa thành key top-level/quote → PyYAML parse OK (giống fix S005 ADR-007: comment note).
2. **#02 MAJOR**: cập nhật `runtime-data.schema.json` theo YAML v2:
   - thêm `status` (string), `frozen` (boolean), `frozen_date` (string, format date) vào properties (giữ `additionalProperties: false`).
   - `aggregate_rules` → items `oneOf` [string, object] (hoặc đổi YAML quote mapping thành string).
   - `classification` → bỏ `required: [manages]`, cho phép các keys phân loại (Runtime Data/Execution Data/Transient/Persistent Metadata/Reference Data) + `not_manages`.
   - `consistency` → đổi `type: array` thành `type: object` `{levels: [...], rules: [...]}`.
   - title cập nhật (vd "S008 v2.1").
3. **#03/#04 MINOR** (tùy chọn cùng ADR): version dồng nhất "1.0.0" + frozen_date → 2026-08-08 (re-freeze).

Sau ADR → re-freeze → **regression lần 2** để gỡ `recheck_required` (hoàn tất TRG-002 cho S008).

## 9. Cập nhật hệ thống (đã thực hiện)

- **Tracker** `review-tracker.yaml`: thêm `review_log` entry REV-20260808-034; **GIỮ** `recheck_required: true` cho S008 (FAIL). Không đổi lifecycle/review.status/count.
- **History** `review-history.yaml`: append entry REV-20260808-034 (unique — 032 bị S017 rev1 lần 2 và 033 bị S003 regression của session song song chiếm trước, precedent REV-20260808-015/016/018/024).
- **SPEC-INDEX.md**: S008 giữ marker ✅ (Frozen/Completed inherited — regression không đổi trạng thái) — khớp tracker.

## 10. id_note

History hiện có 33 entries (001-033). Session song song (S003 regression REV-20260808-033 + S017 rev1 lần 2 REV-20260808-032) chiếm trước — dùng **034** để đảm bảo unique (grep toàn docs xác nhận chưa ai chiếm).
