---
name: review-report
description: >
  REV-20260808-018 — review revfull cho SPEC-001/S005 (Runtime Architecture).
  Không tự sửa — do /review tạo.
agent: general
---

# Review Report — SPEC-001/S005

- **review_id**: REV-20260808-018
- **type**: revfull
- **mode**: revfull-as-health (inherited Frozen) — giữ nguyên 2 trục, không tăng count
- **verdict**: FAIL (Decision Matrix: 1 CRITICAL)
- **lifecycle**: Frozen → **giữ nguyên** · **review.status**: Completed → **giữ nguyên** · **count**: 2 → **2**
- **score**: coverage 95 / quality 60 / consistency 81 / governance 90 / overall **82**
- **coverage**: direct_reference 100 / cross_reference 85 / governance 90 / schemas 20

## Findings (severity: CRITICAL/MAJOR/MINOR/INFO)

| # | Severity | Category | Location | Mô tả | Gợi ý |
|---|----------|----------|----------|-------|-------|
| #01 | CRITICAL | SCHEMA | `S005/architecture.yaml:125-130` (`communication_rules`) + `:152-159` (`constraints`) | Block sequence + key `note:` cùng indent → PyYAML `ParserError: expected <block end>, but found '?'` (line 126 → 130). **Artifact canonical (`architecture.yaml` = "nguồn dữ liệu chuẩn" theo architecture.md:326) KHÔNG parse được** bằng parser YAML chuẩn — 7/8 YAML của S005 parse OK, duy nhất file canonical FAIL. Git blame xác nhận lỗi có từ commit cacce422 (2026-08-05 22:19, v2) và **đã nằm trong bản freeze 6a18636 (2026-08-05 22:28)** — file hỏng ngay từ lúc freeze, chưa từng được validate bằng máy. Cùng lớp S003 CRITICAL #01 (REV-20260808-008) + S006 CRITICAL #01 (REV-20260808-016). 2 vị trí bị lỗi (communication_rules + constraints). | Bỏ `note:` khỏi block sequence (chuyển thành comment `# Ngoài ra đều bị cấm` / `# Tất cả đều CẤM`) hoặc tách `note` thành key cấp cao khác. Kiểm tra lại bằng PyYAML + jsonschema sau khi fix. |
| #02 | MAJOR | SCHEMA | `S005/architecture.schema.json:47` (`additionalProperties: false`) vs `architecture.yaml:8-9` (`frozen`, `frozen_date`) | Schema top-level không khai báo `frozen` + `frozen_date` trong khi `additionalProperties: false` → strict schema validation **FAIL** (đã xác minh bằng máy trên bản YAML đã sửa cú pháp: `Additional properties are not allowed ('frozen', 'frozen_date')`). Cùng lớp S002 #01 (đã fix ADR-002) + S003 #02 (đã fix ADR-003/004) — S005 chưa được fix. | Thêm `frozen` (boolean) + `frozen_date` (string) vào `properties` (giữ `additionalProperties: false`) rồi bump schema version — theo mẫu ADR-002 PATCH. |
| #03 | MAJOR | TRACEABILITY | `S005/architecture.md:280` vs `architecture-registry.yaml` (17 dòng) | md claim "Layer × Responsibility, Layer × Requirement xem `architecture-registry.yaml` (Doctor đọc YAML)" nhưng registry **không chứa** 2 mapping đó — chỉ có version/layers/domains/views/principles_mapping → tham chiếu dẫn tới nội dung không tồn tại (dangling claim, POLICY-011 cross-reference). | Bổ sung `layer_responsibility` + `layer_requirement` vào `architecture-registry.yaml` (kèm dữ liệu từ layer-model/architecture.yaml) hoặc bỏ câu claim khỏi md. |
| #04 | MINOR | CONSISTENCY | Version không nhất quán 8 artifact | `architecture.yaml` + `architecture.md:12` = **1.1.0**; `architecture-registry.yaml`/`layer-model.yaml`/`domain-model.yaml`/`architecture-matrix.yaml` = **1.1**; `dependency-rules.yaml`/`communication-rules.yaml`/`architecture-decision-log.yaml` = **1.0** → 3 cấp version khác nhau giữa các artifact cùng mục (POLICY-002). | Thống nhất version về 1.1.0 (hoặc 1.0.0) cho toàn bộ artifact khi mở S005 fix (tiền lệ S013 #04 / S012 #02). |
| #05 | MINOR | CONSISTENCY | `architecture.yaml:101-119` (domains) vs `domain-model.yaml` + `architecture.md` A005 | `architecture.yaml` domains chỉ có `owner` + `contains_business_logic` — **thiếu key `layers`** của từng domain trong khi domain-model.yaml (L8-29) + architecture.md A005 (L124-133) đều map domain → layers → bất nhất giữa file canonical và Domain View. | Bổ sung `layers` cho từng domain trong `architecture.yaml` (Execution: [Command, Workflow, Execution], Coordination: [Coordination], ...) khớp domain-model.yaml. |
| #06 | MINOR | CONSISTENCY | `architecture.yaml:9` | `frozen_date: "2026-08-04"` nhưng commit freeze cuối sửa nội dung là 6a18636 (**2026-08-05 22:28**) — frozen_date trước lần sửa cuối, metadata freeze không nhất quán (Frozen = không sửa sau freeze). Tiền lệ S003 #04. | Cập nhật `frozen_date` thành 2026-08-05 kèm ADR ghi nhận (hoặc ghi chú changelog). |
| #07 | INFO | TRACEABILITY | `architecture.yaml:141` + `architecture.md:216-226` (Dependency View) | `dependency_view: [Workflow, Execution, Capability, Registry]` — "Registry" không phải layer trong 9-layer model (9 layer: Command..Publication); là input của Resolution layer / component S014 → view rút gọn hợp lý nhưng tên không cùng namespace. | Ghi chú rõ "Registry = S014 (Runtime Registry)" khi fix để tránh hiểu nhầm là layer thứ 10. |
| #08 | INFO | DEPENDENCY | `architecture.md:253` | "Ba sơ đồ sau là những gì Dashboard sẽ dùng" — forward reference tới S020 Runtime Dashboard (intentional — S020 sẽ dùng Layer/Dependency/DataFlow/EventFlow View) — ghi nhận, không phải lỗi. | Không cần sửa. |
| #09 | INFO | ARCHITECTURE | `architecture.md:182-194` (A009) | Mapping A009 có tầng "Services" giữa Components → Contracts trong khi S005 chỉ định nghĩa 9 layers + 6 domains (không có "Services") — ghi nhận để S006 (Components) / S007 (Contracts) xác nhận khi resolve mapping. | Không cần sửa ngay; xác nhận lại khi S006/S007 fix. |

**Xác nhận tích cực:** Nội dung S005 rất hoàn chỉnh — 9 layers đúng trách nhiệm/không chồng chéo, 6 domains mỗi domain 1 owner, 4 views (Layer/Dependency/DataFlow/EventFlow) đầy đủ, dependency rules N→N+1, 4 communication channels, 7 invariants, A000 6 decisions khớp `decisions` trong YAML, quality/stability/metrics (layer_count 9, domain_count 6, max_layer_dependency 1, circular_dependency 0) khớp thực tế, Layer × Principle và Layer × Boundary **khớp 100%** giữa md ↔ architecture.yaml ↔ architecture-matrix.yaml, mọi principle P001/P002/P003/P005/P007/P008/P009/P010/P012/P014/P020 tồn tại trong SPEC-000 (03-principles.md), mọi boundary B001-B007 tồn tại trong S004 (boundaries.md + boundaries.yaml), 7/8 YAML parse OK, schema top-level keys + `layers` patternProperties + `principle` pattern `^P\d{3}$` + status enum đều hợp lệ. Không có circular dependency (max_layer_dependency 1).

## Cascade Review

- cascade_review: **[S006, S007, SPEC-002/W005]** — mục phụ thuộc/tham chiếu S005:
  - **S006** (Components): `components.md:265` tham chiếu trực tiếp `../S005/architecture.yaml` (canonical đang hỏng YAML) + "mỗi cmp thuộc 1 Layer + 1 Domain (S005)" — cần regression sau khi S005 fix. **Đã có `recheck_required: true`** (từ REV-20260808-008/016) — giữ nguyên.
  - **S007** (Contracts): `contracts.md:277` tham chiếu `../S005/architecture.yaml` — **đã có `recheck_required: true`** — giữ nguyên.
  - **SPEC-002/W005** (Workflow Architecture): mirror cấu trúc S005 (`architecture.md:52,143` — "Mirror S005", ref `../../SPEC-001/S005/architecture.yaml` làm mẫu cấu trúc) — SPEC-002 chưa khởi tạo tracker, cần kiểm tra khi start.
- Tracker: gắn `recheck_required: true` cho **S005** (verdict FAIL — còn CRITICAL chưa fix, chờ regression TRG-002). **KHÔNG đổi** review.status các mục khác.

## Trigger nhắc

- SLA: S005 Frozen — draft_days 0 (không áp dụng TRG-006) · frozen 2026-08-04 → stale sau ~2027-01-31 (TRG-007 còn ~176 ngày — chưa kích hoạt).
- TRG-005 sẽ kích hoạt khi schema.json được fix (migration review sau khi áp dụng fix).

## System Analysis (revfull)

- **cascade_impact**: #01 (invalid YAML) + #02 (schema strict FAIL) đều là lỗi **nội tại của S005** — nội dung dữ liệu đúng (9 layers/6 domains đầy đủ, md ↔ YAML cross-mapping khớp), chỉ hỏng ở tầng serialize/validate → fix không đổi contract dữ liệu, downstream ít bị ảnh hưởng (S006/S007 đọc `architecture.yaml` cần recheck; SPEC-002/W005 mirror khi start).
- **upgrade_suggestions**:
  - `architecture.yaml`: bỏ/tách 2 key `note:` khỏi block sequence (communication_rules L130 + constraints L159) — **P1** (chặn parser FAIL, canonical đang không dùng được bằng máy — cùng lớp S003 #01/S006 #01).
  - `architecture.schema.json`: thêm `frozen` + `frozen_date` vào properties — **P1** (cùng lớp S002 #01 đã fix ADR-002; validator đang FAIL).
  - `architecture-registry.yaml` + `architecture.md:280`: bổ sung layer_responsibility/layer_requirement hoặc bỏ claim — **P2** (POLICY-011 traceability).
  - Version thống nhất 1.1.0 + `frozen_date` 2026-08-05 + domains.layers — **P2** (POLICY-002, metadata).
- **missing_items**: S005 đủ deliverable (10 file: architecture.md + 8 yaml + schema.json). Không có `CHANGELOG.md` riêng cho S005 (tiền lệ S002 có) — gợi ý thêm khi fix metadata.
- **recommended_next**: (1) Fix #01–#02 qua RFC/ADR (S005 Frozen — cần approval, theo precedent ADR-002 của S002 / ADR-003,004 của S003); (2) chạy `/doctor` cho regression TRG-002 lên S006/S007; (3) sau khi fix, chạy `/review SPEC-001/S005 health` để xác nhận và gỡ recheck_required.

## Summary & Next Step

S005 (Runtime Architecture) có nội dung chất lượng cao — 9 layers/6 domains/4 views đầy đủ, cross-mapping md ↔ YAML ↔ matrix khớp 100%, mọi tham chiếu P0xx/B0xx/S004/S006 tồn tại — nhưng **artifact canonical hỏng nghiêm trọng**: `architecture.yaml` không parse được do block sequence + `note:` cùng indent ở 2 vị trí (CRITICAL #01, có từ lúc freeze 2026-08-05, chưa từng machine-validated — đúng lớp S003/S006) và schema strict FAIL vì `additionalProperties: false` chặn `frozen`/`frozen_date` (MAJOR #02 — cùng lớp S002 đã fix). Kèm 1 MAJOR traceability #03 (registry thiếu nội dung md claim). Verdict **FAIL** theo Decision Matrix (1 CRITICAL). Do mục Frozen inherited + verdict FAIL, trạng thái 2 trục giữ nguyên (lifecycle Frozen, review.status Completed, count 2, inherited); gắn `recheck_required: true` cho S005 chờ fix.

**next_step**: Lập RFC/ADR (theo mẫu ADR-002/003/004) để fix #01–#02 (P1): tách 2 key `note:` khỏi block sequence trong `architecture.yaml` + bổ sung `frozen`/`frozen_date` vào schema → sau khi chấp thuận và fix → chạy `/review SPEC-001/S005 health` xác nhận + `/doctor` cho cascade S006/S007.
