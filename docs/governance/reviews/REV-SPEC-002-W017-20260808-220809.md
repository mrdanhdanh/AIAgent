---
review_id: REV-20260808-102
spec: SPEC-002
item: W017
name: Workflow Extensions
type: rev1
verdict: CONDITIONAL
date: "2026-08-08"
report_of: docs/governance/reviews/REV-SPEC-002-W017-20260808-220809.md
---

# Review REV-20260808-102 — SPEC-002/W017 Workflow Extensions (review lần 1)

## Context

- **Target**: `docs/specs/SPEC-002/W017/extensions.md` (17 sections WXE001–WXE017) + 9 YAML artifacts + `workflow-extensions.schema.json`.
- **Type**: `rev1` — **review chính thức lần 1 (tính count)**. Mục thứ 17 trong chuỗi review SPEC-002, `recheck_required: true` do cascade W007/W009/W011/W012/W014/W015/W016 (TRG-002).
- **Trạng thái trước review**: lifecycle `Draft`, review.status `NotReviewed`, count `0`, `recheck_required: true`.
- **review_id**: REV-20260808-102. Xác nhận chưa bị session song song chiếm: history = 100 entries (mới nhất 101), review_log mới nhất = REV-20260808-101 (W016), grep `REV-20260808-102` toàn docs (tracker + history + reports + git log) = 0 matches → **102 khả dụng** (entries.length 100 + 1 = 101 đã bị W016 RE-REVIEW chiếm → dùng 102, tiền lệ REV-20260808-015/016/018/023).
- **Đã biết trước (grep)**: W017 `extensions.md:109` có claim "S011 reuse trực tiếp" + `workflow-extension-events.yaml:1` ghi "(S011 reuse)" — kiểm tra và đưa vào findings nếu đúng.

## SLA (P3)

- TRG-006 (Draft > 30 ngày): **không áp** — draft_days = 0 (created 2026-08-08).
- TRG-007 (Completed > 180 ngày): **không áp** — chưa Completed.
- Target `Draft/NotReviewed/count 0` (không inherited, không Completed) → **review bình thường**, type rev1 tính count.

## Verify máy (PyYAML 6.0.3 + jsonschema 4.26.0 + grep + git)

- **git diff W017 vs HEAD**: sạch — 0 modified (commit tạo 363366a, không có thay đổi chưa commit) → review trên nội dung đã commit, không nhiễu.
- **YAML parse**: 9/9 PASS, 0 ParserError (W017 SẠCH lớp CRITICAL — không lặp lại lỗi `note:`/block-mapping của W005/W006/W008/W009/W010/W011/W012/W014/W015).
- **jsonschema**: `workflow-extensions.yaml` vs `workflow-extensions.schema.json` Draft202012 strict **VALID 0 errors** + schema **META-VALID** (required 17 keys = schema required 17, additionalProperties: false, categories minItems 5, lifecycle minItems 6, installation minItems 4).
- **grep**: `reuse` toàn W017 = 2 matches — cả 2 đều nằm ở claim event (extensions.md:109 + workflow-extension-events.yaml:1) → **MAJOR #01 xác nhận ĐÚNG như dự kiến**.
- **Single-definition**: `WORKFLOW_EXTENSION_*` chỉ xuất hiện trong W017 (extensions.md:88/107, workflow-extensions.yaml:59-64, workflow-extension-events.yaml:5-10, workflow-extension-isolation.yaml:9) — 0 collision với W009/W013/W014/W016.

## Coverage / Content đầy đủ

- 17 sections WXE001–WXE017 contiguous khớp frontmatter (philosophy 4, principles 6, categories 5, extension_model 10 fields, lifecycle 6, installation 4, activation_rules 4, isolation 4, security 2, resources 2, compatibility 4).
- **Events 6 — 3 nguồn khớp**: md WXE014 = canonical `events` 6 = `workflow-extension-events.yaml` 6 (`WORKFLOW_EXTENSION_REGISTERED/INSTALLED/ACTIVATED/DISABLED/FAILED/UNINSTALLED`).
- **Metrics 6**: canonical `metrics` 6 = `workflow-extension-metrics.yaml` 6 (active/installed/failed_extensions, extension_calls/errors, avg_extension_latency).
- **Categories 5** = `workflow-extension-categories.yaml` 5; **lifecycle 6** = `workflow-extension-lifecycle.yaml` 6; **installation 4** = `workflow-extension-installation.yaml` flow 4; **extension_model 10 fields** = `workflow-extension-model.yaml` 10.
- **Machine-readable WXE016**: list 10 = thực tế 10 (9 YAML + schema.json), 0 missing 0 extra.

## Cross-reference 2 chiều — 0 BAD

- **Upstream** (đều tồn tại): W007 contracts.md (Contract First), W012 policies.md (Binding Extension), W013 governance.md (Compatibility/Governance/Deny mặc định S013 GV008 + Invalid Binding → Audit S011), W014 registry.md (Registry compatibility check, Resolution), W015 resources.md (quota), W016 compliance.md (Verifiable), S010 EF008 (Context Flow), S011 OB003A (Observability Boundary) + events.yaml model có `correlation_id`, S012 POL-ISOL-001 + POL-SEC-001 (isolation-policy.yaml + security-policy.yaml), S014 plugin-registry.yaml, S017 plugins.md (mẫu mirror).
- **Downstream**: W019 doctor.md:33/46/152 + workflow-doctor.yaml:31/44 + workflow-doctor-checks.yaml:15 + workflow-doctor-scope.yaml:12 tham chiếu W017 — đều tồn tại. W018/W020 không ref trực tiếp W017 (không phải bad ref).
- **SPEC.yaml** includes W017 ✓.

## Findings

### MAJOR #01 — TRACEABILITY (POLICY-011): claim "S011 reuse trực tiếp" sai nguồn ✅ xác nhận ĐÚNG dự kiến

- **Vị trí**: `extensions.md:109` (`> S011 reuse trực tiếp.` — dưới WXE014 Extension Events) + `workflow-extension-events.yaml:1` (`# SPEC-002 W017 - Extension Events (S011 reuse)`).
- **Bằng chứng**: S011 `events.yaml` chỉ định nghĩa **event model** (`model.fields: [id, type, category, severity, source, correlation_id, ...]` + taxonomy `event_type: [Execution, Workflow, Capability, Resource, Policy, System]`) — KHÔNG định nghĩa 6 event type `WORKFLOW_EXTENSION_*` cụ thể nào (grep WORKFLOW_EXTENSION_ toàn S011 = 0 matches). Tuyên bố "reuse trực tiếp" không kiểm chứng được.
- **Cùng lớp**: W013#01 / W014#02 / W011#03 / W012#08 / W015#03 / W016#01 — đều đã fix theo mẫu.
- **Đề nghị**: `extensions.md:109` → `> W017 định nghĩa 6 event types WORKFLOW_EXTENSION_* — S011 cung cấp event model (fields, correlation_id).`; `workflow-extension-events.yaml:1` → `# SPEC-002 W017 - Extension Events (6 event types WORKFLOW_EXTENSION_* — S011 cung cap event model fields, correlation_id)`.

### MINOR #02 — version `1.0` ở 8 artifact phụ vs `1.0.0` canonical (kế thừa, không chặn)

- 8 artifact phụ (events, model, categories, lifecycle, installation, isolation, metrics, validation) ghi `version: "1.0"` trong khi canonical `workflow-extensions.yaml` + md ghi `1.0.0`. Không chặn (tiền lệ W016 #03 / W013 #03).

### INFO #03 / #04 / #05 — kế thừa, không chặn

- INFO #03: frontmatter `extensions.md` thiếu key `version`.
- INFO #04: schema `status` enum `[Draft, Review, Approved, Stable, Deprecated]` thiếu `Frozen` — chờ auto-freeze.
- INFO #05: header md `extensions.md:12` "Trạng thái: Draft" sẽ lệch lifecycle `Review` sau review này — tự hết khi Approved/Frozen.

## Scoring

- **coverage**: 97 (nội dung đầy đủ, machine-readable 10/10, sections 17/17, events 3 nguồn khớp; trừ -3 MINOR #02 version)
- **quality**: 97 (cấu trúc rõ ràng, philosophy/principles/lifecycle/installation nhất quán md↔yaml; trừ -3 MINOR #02)
- **consistency**: 90 (cross-ref 2 chiều 0 BAD, single-definition 0 collision; trừ -10 MAJOR #01 traceability claim sai nguồn)
- **governance**: 90 (naming chuẩn, schema META-VALID, canonical version 1.0.0; trừ -10 MAJOR #01 POLICY-011)
- **overall**: 93 = round((97+97+90+90)/4)

- **coverage chi tiết**: direct_reference 97, cross_reference 90, governance 90, schemas 100.

## Verdict — CONDITIONAL (Decision Matrix: 0 CRITICAL, 1 MAJOR → CONDITIONAL)

- **Tăng count (type tính count + verdict PASS/CONDITIONAL)**: `NotReviewed → Rev1`, count `0 → 1`, lifecycle `Draft → Review` (auto khi count ≥ 1).
- **KHÔNG auto-freeze** (type rev1 — freeze_rule chỉ áp cho revfull PASS + Completed).
- `recheck_required` W017 giữ **true** (còn MAJOR #01 chưa fix — "con gi treo", chờ regression/Doctor TRG-002 sau khi fix).
- Thêm entry `history[]` W017 + `review_log` (REV-20260808-102, rev1, CONDITIONAL).

## Cascade review

- Mục tham chiếu W017: **W019** (doctor.md x4 — ref trực tiếp), **W018/W020** (tier Experience, không ref trực tiếp nhưng cùng nhóm) — **đều đã có `recheck_required: true` từ cascade trước**, giữ nguyên, **không đổi review.status** (TRG-002 — Doctor lập task regression).
- Không cascade mới: MAJOR #01 chỉ sửa claim metadata (không đổi event types / key / contract) → downstream không bị ảnh hưởng.

## Ghi chú

- 1 MAJOR #01 (claim S011 reuse) — xác nhận ĐÚNG như dự kiến từ grep trước review; cùng lớp lỗi đã fix ở W013/W014/W011/W012/W015/W016.
- Báo cáo chỉ ghi nhận + gợi ý; không tự sửa nội dung mục.

## Next step

- User fix MAJOR #01 (2 vị trí claim) theo đề nghị, sau đó chạy lại review (RE-REVIEW xác nhận fix, không tính count) → rồi review lần 2 (rev1 thứ 2 tính count) → Rev1 → Completed.
- Mục tiếp theo trong chuỗi SPEC-002: **W018 (Workflow Evolution)** — rev1 (đang Draft/NotReviewed/count 0).
- Khuyến nghị chạy `/doctor` khi muốn kiểm tra health tổng thể + regression TRG-002.
