# SPEC-015 X009 - SDK Transitions (5)
---
version: "1.0.0"
transitions:
  XTR-001: { from: XST-001, to: XST-002, event: SDK_RENDERING, guard: View hop le }
  XTR-002: { from: XST-002, to: XST-003, event: SDK_RENDERED, guard: Render xong }
  XTR-003: { from: XST-002, to: XST-006, event: SDK_FAILED, guard: Render fail }
  XTR-004: { from: XST-003, to: XST-004, event: SDK_FILTERED, guard: Filter xong }
  XTR-005: { from: XST-004, to: XST-005, event: SDK_ARCHIVED, guard: Het retention }
