# SPEC-015 X009 - SDK Events (5)
---
version: "1.0.0"
events:
  SDK_RENDERING: { from: XST-001, to: XST-002, category: lifecycle }
  SDK_RENDERED: { from: XST-002, to: XST-003, category: lifecycle }
  SDK_FAILED: { from: XST-002, to: XST-006, category: lifecycle }
  SDK_FILTERED: { from: XST-003, to: XST-004, category: lifecycle }
  SDK_ARCHIVED: { from: XST-004, to: XST-005, category: lifecycle }
note: Event immutable, append-only (S011 P010).
