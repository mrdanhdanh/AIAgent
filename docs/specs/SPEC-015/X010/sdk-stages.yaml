# SPEC-015 X010 - SDK Stages Registry
---
version: "1.0.0"
registered_stages:
  XF-001: { name: Create, actor: SDK, event: SDK_CREATED }
  XF-002: { name: Render, actor: RenderEngine, event: SDK_RENDERING }
  XF-003: { name: Compose, actor: SDK, event: SDK_COMPOSED }
  XF-004: { name: Filter, actor: FilterEngine, event: SDK_FILTERED }
  XF-005: { name: Refresh, actor: RefreshEngine, event: SDK_REFRESHED }
  XF-006: { name: Export, actor: ExportEngine, event: SDK_EXPORTED }
