# SPEC-015 X011 - SDK Audit
---
version: "1.0.0"
audit:
  who: user_id / engine
  what: op (Create/Render/run/Compose/Filter/Export)
  when: timestamp
  where: SDK_id, execution_id
  why: transition_id, event
  audit_log: append-only (S011)
  source: XFR-016
