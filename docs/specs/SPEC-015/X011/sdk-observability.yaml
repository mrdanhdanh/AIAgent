# SPEC-015 X011 - SDK Observability Model
---
version: "1.0.0"
observability:
  logs: SDK Engine logs (structured)
  events: SDK_* (S011 Event Store)
  metrics: SDK_* (prometheus)
  traces: trace_id + correlation_id (S011)
boundary: SDK la observable (RULE-014)
