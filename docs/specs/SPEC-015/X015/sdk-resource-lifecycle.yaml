# SPEC-015 X015 - SDK Resource Lifecycle
---
version: "1.0.0"
lifecycle:
  states: [allocate -> in_use -> archived]
  leaked: resource khong duoc release -> SDK X019 phat hien
  reclamation: archive theo retention (XPOL-006)
