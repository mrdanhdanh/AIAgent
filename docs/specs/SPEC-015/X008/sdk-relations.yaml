# SPEC-015 X008 - SDK Relations
---
version: "1.0.0"
relations:
  REL-X001: { from: SDK, relation: owns, to: SDKDefinition, cardinality: 1..1 }
  REL-X002: { from: SDK, relation: renders, to: Widget, cardinality: 0..* }
  REL-X003: { from: SDK, relation: composes, to: Panel, cardinality: 0..* }
  REL-X004: { from: SDK, relation: builds, to: SDKView, cardinality: 0..* }
  REL-X005: { from: SDK, relation: has, to: SDKState, cardinality: 1..1 }
  REL-X006: { from: SDK, relation: exports, to: SDKExport, cardinality: 0..1 }
  REL-X007: { from: SDK, relation: refers, to: ExecutionRef, cardinality: 1..1 }
  REL-X008: { from: SDK, relation: emits, to: SDKEvent, cardinality: 0..* }
  REL-X009: { from: SDK, relation: snapshots, to: SDKSnapshot, cardinality: 0..* }
