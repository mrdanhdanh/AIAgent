# SPEC-009 X008 - Contract Relations
---
version: "1.0.0"
relations:
  REL-X001: { from: Contract, relation: owns, to: ContractDefinition, cardinality: 1..1 }
  REL-X002: { from: Contract, relation: owns, to: ContractVersion, cardinality: 1..* }
  REL-X003: { from: Contract, relation: owns, to: ContractSchema, cardinality: 1..1 }
  REL-X004: { from: Contract, relation: binds, to: ContractBinding, cardinality: 0..* }
  REL-X005: { from: Contract, relation: has, to: ContractState, cardinality: 1..1 }
  REL-X006: { from: Contract, relation: verified, to: ContractVerification, cardinality: 0..* }
  REL-X007: { from: Contract, relation: refers, to: ExecutionRef, cardinality: 1..1 }
  REL-X008: { from: Contract, relation: emits, to: ContractEvent, cardinality: 0..* }
  REL-X009: { from: Contract, relation: snapshots, to: ContractSnapshot, cardinality: 0..* }
