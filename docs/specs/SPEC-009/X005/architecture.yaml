# SPEC-009 X005 - Contract Architecture
---
version: "1.0.0"
architecture:
  model: Layered (5 layers)
  style: In-process + Store
  key_decisions:
    - Contract interface-only (TERM-014)
    - Version immutable (P004)
    - Policy qua S012
