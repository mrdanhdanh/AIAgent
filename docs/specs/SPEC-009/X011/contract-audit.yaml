# SPEC-009 X011 - Contract Audit
---
version: "1.0.0"
audit:
  who: provider_id / caller_id / engine
  what: op (declare/version/resolve/verify/retire)
  when: timestamp
  where: contract_id, execution_id
  why: transition_id, event
  audit_log: append-only (S011)
  source: XFR-015
