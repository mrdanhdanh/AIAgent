# SPEC-009 X012 - Retry Binding
---
version: "1.0.0"
retry_binding:
  retry: theo S012 retry policy
  applies_to: [Publish, Resolve, Verify, Retire]
  max_attempts: 3 (default)
  backoff: exponential (S012)
