# SPEC-009 X010 - Contract Resolution
---
version: "1.0"
resolution:
  input: contract_id + version
  output: Contract + schema hoac not-found
  compat_check: verify truoc khi tra ve
  trace: moi resolution co trace_id (S011)
