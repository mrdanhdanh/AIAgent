# SPEC-009 X018 - Contract Migration
---
version: "1.0.0"
migration:
  - Upgrade: bo sung field/op (backward)
  - Downgrade: giu field cu (compat)
  - Breaking: can ADR + RFC + migration doc
  - Runtime Contract: KHONG migrate - version moi (P004)
