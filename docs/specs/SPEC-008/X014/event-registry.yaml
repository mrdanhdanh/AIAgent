# SPEC-008 X014 - Event Registry (via SPEC-005)
---
version: "1.0.0"
registry:
  owner: Registry (SPEC-005)
  entries: Event Definition (schemas, state machine, policies)
  runtime_data: KHONG vao Registry (Event o Store)
  entry_types:
    - EventDefinition
    - EventSchema
    - EventPolicy
    - EventStateMachine
  resolution: Registry resolve, Runtime thuc thi (RB004)
