# SPEC-008 X012 - Retry Binding
---
version: "1.0"
retry_binding:
  retry: theo S012 retry policy
  applies_to: [Store, Route, Deliver, Archive]
  max_attempts: 3 (default)
  backoff: exponential (S012)
