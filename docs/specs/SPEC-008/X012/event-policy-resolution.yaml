# SPEC-008 X012 - Event Policy Resolution
---
version: "1.0.0"
resolution:
  order: SPEC-005 registry (policy registry)
  conflict: deny wins (S012)
  scope: event op (publish/deliver/replay)
  cache: khong cache policy decision (evaluate moi lan)
