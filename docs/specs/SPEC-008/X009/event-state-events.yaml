# SPEC-008 X009 - Event Events (6)
---
version: "1.0"
events:
  EVENT_VALIDATING: { from: XST-001, to: XST-002, category: lifecycle }
  EVENT_PUBLISHED: { from: XST-002, to: XST-003, category: lifecycle }
  EVENT_REJECTED: { from: XST-002, to: XST-006, category: lifecycle }
  EVENT_DELIVERED: { from: XST-003, to: XST-004, category: lifecycle }
  EVENT_ARCHIVED: { from: XST-004, to: XST-005, category: lifecycle }
  EVENT_ARCHIVED: { from: XST-003, to: XST-005, category: lifecycle }
note: Event immutable, append-only (S011 P005).
