# SPEC-008 X011 - Event Audit
---
version: "1.0.0"
audit:
  who: producer_id / subscriber_id / engine
  what: op (publish/route/deliver/replay/archive)
  when: timestamp
  where: event_id, execution_id
  why: transition_id, event
  audit_log: append-only (S011)
  source: XFR-016
