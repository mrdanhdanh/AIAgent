# SPEC-005 R012 - Registry Policy Traceability (S012 RP015)
---
version: "1.0"
records:
  fields: [binding_id, policy_ref, entry_id, execution_id, correlation_id, parameters, result, timestamp]
rules:
  - Moi apply sinh Event + Audit (S011).
