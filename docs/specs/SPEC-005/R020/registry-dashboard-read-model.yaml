# SPEC-005 R020 - Registry Dashboard Read Model
---
version: "1.0"
read_model:
  fields: [id, timestamp, registry_version, views, sources, refresh]
rules:
  - Read model la projection immutable (P010).
