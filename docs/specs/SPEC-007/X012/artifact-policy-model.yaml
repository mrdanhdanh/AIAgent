# SPEC-007 X012 - Artifact Policy Model
---
version: "1.0"
policy_model:
  identity: policy_id (XPOL-###)
  enforcement: Artifact Manager (before op)
  evaluation: metadata-only (khong doc business data)
  result: allow | deny | warn
  trace: policy decision log (S011)
