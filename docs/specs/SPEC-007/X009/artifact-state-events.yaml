# SPEC-007 X009 - Artifact Events (7)
---
version: "1.0.0"
events:
  ARTIFACT_VALIDATING: { from: XST-001, to: XST-002, category: lifecycle }
  ARTIFACT_PUBLISHED: { from: XST-002, to: XST-003, category: lifecycle }
  ARTIFACT_REJECTED: { from: XST-002, to: XST-006, category: lifecycle }
  ARTIFACT_CONSUMED: { from: XST-003, to: XST-004, category: lifecycle }
  ARTIFACT_VERSIONED: { from: XST-003, to: XST-003, category: lifecycle }
  ARTIFACT_ARCHIVED:
    - { from: XST-004, to: XST-005, category: lifecycle }
    - { from: XST-003, to: XST-005, category: lifecycle }
note: Event immutable, append-only (S011 P010).
