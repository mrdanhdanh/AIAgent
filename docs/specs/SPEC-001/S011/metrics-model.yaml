# SPEC-001 S011 — Metrics Model (tham chieu S010 EF010)
---
version: "1.0"
model:
  fields: [id, name, value, unit, correlation_id, execution_id, timestamp, tags]
rules:
  - Metrics chỉ append.
  - Không chứa Business Data (S008 D006).
  - Metrics phát sau Trace (S010 EF010).
