# SPEC-010 X009 - Plugin Events (6)
---
version: "1.0"
events:
  PLUGIN_VALIDATING: { from: XST-001, to: XST-002, category: lifecycle }
  PLUGIN_ENABLED: { from: XST-002, to: XST-003, category: lifecycle }
  PLUGIN_REJECTED: { from: XST-002, to: XST-006, category: lifecycle }
  PLUGIN_DISABLED: { from: XST-003, to: XST-004, category: lifecycle }
  PLUGIN_ENABLED: { from: XST-004, to: XST-003, category: lifecycle }
  PLUGIN_UNINSTALLED: { from: XST-004, to: XST-005, category: lifecycle }
note: Event immutable, append-only (S011 P005).
