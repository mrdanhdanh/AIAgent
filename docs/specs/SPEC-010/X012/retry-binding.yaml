# SPEC-010 X012 - Retry Binding
---
version: "1.0"
retry_binding:
  retry: theo S012 retry policy
  applies_to: [Enable, Export, Disable, Uninstall]
  max_attempts: 3 (default)
  backoff: exponential (S012)
