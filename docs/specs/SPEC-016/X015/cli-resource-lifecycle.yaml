# SPEC-016 X015 - CLI Resource Lifecycle
---
version: "1.0.0"
lifecycle:
  states: [allocate -> in_use -> archived]
  leaked: resource khong duoc release -> CLI X019 phat hien
  reclamation: archive theo retention (XPOL-006)
