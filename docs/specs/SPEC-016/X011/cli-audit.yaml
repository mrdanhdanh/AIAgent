# SPEC-016 X011 - CLI Audit
---
version: "1.0.0"
audit:
  who: user_id / engine
  what: op (Create/Render/run/Compose/Filter/Export)
  when: timestamp
  where: CLI_id, execution_id
  why: transition_id, event
  audit_log: append-only (S011)
  source: XFR-016
