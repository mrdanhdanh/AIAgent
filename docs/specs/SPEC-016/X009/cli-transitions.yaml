# SPEC-016 X009 - CLI Transitions (5)
---
version: "1.0.0"
transitions:
  XTR-001: { from: XST-001, to: XST-002, event: CLI_RENDERING, guard: View hop le }
  XTR-002: { from: XST-002, to: XST-003, event: CLI_RENDERED, guard: Render xong }
  XTR-003: { from: XST-002, to: XST-006, event: CLI_FAILED, guard: Render fail }
  XTR-004: { from: XST-003, to: XST-004, event: CLI_FILTERED, guard: Filter xong }
  XTR-005: { from: XST-004, to: XST-005, event: CLI_ARCHIVED, guard: Het retention }
