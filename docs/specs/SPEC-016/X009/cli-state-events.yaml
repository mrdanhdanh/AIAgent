# SPEC-016 X009 - CLI Events (5)
---
version: "1.0.0"
events:
  CLI_RENDERING: { from: XST-001, to: XST-002, category: lifecycle }
  CLI_RENDERED: { from: XST-002, to: XST-003, category: lifecycle }
  CLI_FAILED: { from: XST-002, to: XST-006, category: lifecycle }
  CLI_FILTERED: { from: XST-003, to: XST-004, category: lifecycle }
  CLI_ARCHIVED: { from: XST-004, to: XST-005, category: lifecycle }
note: Event immutable, append-only (S011 P010).
