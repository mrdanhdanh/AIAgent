# SPEC-016 X014 - CLI Registry (via SPEC-005)
---
version: "1.0.0"
registry:
  owner: Registry (SPEC-005)
  entries: CLI Definition (scanners, schemas, policies)
  runtime_data: KHONG vao Registry (Results o Store)
  entry_types:
    - CLIDefinition
    - ScannerDefinition
    - CLIPolicy
    - CLIStateMachine
  resolution: Registry resolve, Runtime thuc thi (RB004)
