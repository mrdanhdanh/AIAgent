# SPEC-016 X012 - Timeout Binding
---
version: "1.0.0"
timeout_binding:
  Create_timeout: 1s (default)
  Render_timeout: 1s (default)
  run_timeout: 30s (default)
  Export_timeout: 5s (default)
  note: cau hinh qua Policy S012, khong hardcode
