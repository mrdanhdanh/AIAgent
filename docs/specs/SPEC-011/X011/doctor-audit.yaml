# SPEC-011 X011 - Doctor Audit
---
version: "1.0"
audit:
  who: user_id / engine
  what: op (scan/diagnose/score/repair/report)
  when: timestamp
  where: scan_id, execution_id
  why: transition_id, event
  audit_log: append-only (S011)
  source: XFR-016
