# SPEC-011 X011 - Doctor Observability Model
---
version: "1.0.0"
observability:
  logs: Doctor logs (structured)
  events: DOCTOR_* (S011 Event Store)
  metrics: doctor_* (prometheus)
  traces: trace_id + correlation_id (S011)
boundary: Doctor la observable (RULE-014)
