# SPEC-002 W013 - Workflow Governance Decision (S013 GV012)
---
version: "1.0"
model:
  fields: [id, workflow, rule, result, reason, timestamp, severity]
result: [Allow, Deny, Suspend, Retry, Escalate]
modes: [Strict, Compatible, Simulation, Audit Only]
