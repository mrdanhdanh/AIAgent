# SPEC-002 W020 - Workflow Dashboard Read Model
---
version: "1.0"
read_model:
  fields: [id, timestamp, workflow_version, views, sources, refresh]
rules:
  - Read model la projection immutable (P010).
