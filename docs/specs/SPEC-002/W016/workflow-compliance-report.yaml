# SPEC-002 W016 - Workflow Compliance Report
---
version: "1.0"
report:
  fields: [id, timestamp, workflow_version, matrix, score, status, certification, evidence]
rules:
  - Report immutable (P005).
