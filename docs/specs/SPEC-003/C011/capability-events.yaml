# SPEC-003 C011 - Capability Events (event model: S011)
---
version: "1.0.0"
definition_events:
  # nguon: C009 capability-state-events.yaml (6 event types)
  - CAPABILITY_VALIDATING
  - CAPABILITY_PUBLISHED
  - CAPABILITY_REJECTED
  - CAPABILITY_DEPRECATED
  - CAPABILITY_REACTIVATED
  - CAPABILITY_RETIRED
run_events:
  note: Capability Run dung EXECUTION_* cua S011 (Runtime).
rules:
  - Event immutable (P010).
  - Event chi append.
  - Moi transition sinh Event (C009).
  - Event co correlation_id.
