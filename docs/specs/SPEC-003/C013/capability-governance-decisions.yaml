# SPEC-003 C013 - Capability Governance Decision (S013 GV012)
---
version: "1.0"
model:
  fields: [id, capability, rule, result, reason, timestamp, severity]
result: [Allow, Deny, Suspend, Retry, Escalate]
modes: [Strict, Compatible, Simulation, Audit Only]
