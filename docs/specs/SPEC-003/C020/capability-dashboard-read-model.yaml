# SPEC-003 C020 - Capability Dashboard Read Model
---
version: "1.0"
read_model:
  fields: [id, timestamp, capability_version, views, sources, refresh]
rules:
  - Read model la projection immutable (P010).
