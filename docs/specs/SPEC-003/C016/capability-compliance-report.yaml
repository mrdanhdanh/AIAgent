# SPEC-003 C016 - Capability Compliance Report
---
version: "1.0.0"
report:
  fields: [id, timestamp, capability_version, matrix, score, status, certification, evidence]
rules:
  - Report immutable (P010).
