# SPEC-012 X012 - Timeout Binding
---
version: "1.0"
timeout_binding:
  define_timeout: 1s (default)
  configure_timeout: 1s (default)
  run_timeout: 30s (default)
  report_timeout: 5s (default)
  note: cau hinh qua Policy S012, khong hardcode
