# SPEC-012 X009 - Simulation Events (5)
---
version: "1.0.0"
events:
  SIMULATION_CONFIGURED: { from: XST-001, to: XST-002, category: lifecycle }
  SIMULATION_RUNNING: { from: XST-002, to: XST-003, category: lifecycle }
  SIMULATION_FAILED: { from: XST-002, to: XST-006, category: lifecycle }
  SIMULATION_OBSERVED: { from: XST-003, to: XST-004, category: lifecycle }
  SIMULATION_REPORTED: { from: XST-004, to: XST-005, category: lifecycle }
note: Event immutable, append-only (S011 P010).
