# SPEC-006 X010 - Context Resolution
---
version: "1.0"
resolution:
  input: key_path (contextId/section/item)
  output: item value hoac not-found
  scope_check: grant scope truoc khi tra ve
  trace: moi resolution co trace_id (S011)
