# SPEC-006 X014 - Context Registry (via SPEC-005)
---
version: "1.0.0"
registry:
  owner: Registry (SPEC-005)
  entries: Context Definition (state machine, schemas, policies)
  runtime_data: KHONG vao Registry (Context transient)
  entry_types:
    - ContextDefinition
    - ContextSchema
    - ContextPolicy
    - ContextStateMachine
  resolution: Registry resolve, Runtime thuc thi (RB004)
