# SPEC-004 A013 - Agent Governance Decision (S013 GV012)
---
version: "1.0"
model:
  fields: [id, agent, rule, result, reason, timestamp, severity]
result: [Allow, Deny, Suspend, Retry, Escalate]
modes: [Strict, Compatible, Simulation, Audit Only]
