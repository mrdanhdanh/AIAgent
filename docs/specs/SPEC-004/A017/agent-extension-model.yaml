# SPEC-004 A017 - Canonical Extension Model
---
version: "1.0.0"
extension:
  fields: [id, name, version, category, status, owner, capabilities, contracts, dependencies, metadata]
rules:
  - contracts bat buoc tro den Contract ton tai (A007).
