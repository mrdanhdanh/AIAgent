# SPEC-004 A017 - Extension Installation
---
version: "1.0.0"
flow:
  - Register (S014 plugin-registry)
  - Validate (Contract A007 + Compatibility A013)
  - Install
  - Activate
rules:
  - Contract khong hop le -> Install that bai.
  - Dependency chua resolve -> Install that bai (S014).
