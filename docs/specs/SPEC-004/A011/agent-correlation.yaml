# SPEC-004 A011 - Agent Correlation (S011 reuse)
---
version: "1.0"
model:
  fields: [agent_id, execution_id, correlation_id, lineage_id, parent_id]
rules:
  - Sinh khi Agent Created.
  - Gan vao: Event, Metrics, Trace, Audit.
  - Khong doi trong vong doi (P009).
