---
name: aios-rules-changelog
description: Changelog của AIOS Architecture Rules.
agent: general
---

# AIOS Architecture Rules — Changelog

## v1.0.0 (2026-08-03)

### Added

- **6 rule files** theo D004:
  `layering` (R-LAYER), `dependency` (R-DEP), `communication` (R-COMM),
  `versioning` (R-VER), `state` (R-STATE), `security` (R-SEC).
- **INDEX.yaml** — rules index (file, summary, principles, sources).
- **rules.schema.json** — JSON Schema cho rule frontmatter.

### Changed

- Cập nhật reference principle theo số mới P001-P020.
